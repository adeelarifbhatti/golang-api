apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: golang-api
  labels:
    app: mysql
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  # ClusterIP keeps MySQL internal — not exposed outside the cluster
  type: ClusterIP
